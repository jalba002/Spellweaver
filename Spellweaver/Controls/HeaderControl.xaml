<UserControl x:Class="Spellweaver.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Spellweaver.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- Header -->
    <Grid Background="{StaticResource HeaderBackgroundBrush}">
        <StackPanel Orientation="Horizontal"
            HorizontalAlignment="Center">
            <Image Style="{StaticResource Header.AppIcon}" Source="/Images/BookIcon.png" Margin="5"/>
            <TextBlock Style="{StaticResource Header.Title}" FontSize="30"
           Foreground="{StaticResource HeaderForegroundBrush}" FontFamily="Consolas" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
